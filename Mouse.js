let message = document.getElementById("message");
let msg = "";

/*
// Click event
document.getElementById("target").addEventListener("click", function () {
  //alert('Click event occurred');
  msg = msg + "Click event occurred";
  message.textContent = msg;
});
*/

/*
// Double click event
document.getElementById("target").addEventListener("dblclick", function () {
  msg = msg + "DblClick event occurred";
  message.textContent = msg;
});
*/

/*
// Mouseup event
document.getElementById('target').addEventListener('mouseup', function() {
  //alert('Mouseup event occurred');
  msg = msg + 'Mouseup event occurred';
  message.textContent = msg;
});
*/

/*
// Mousedown event
document.getElementById('target').addEventListener('mousedown', function() {
  //alert('Mousedown event occurred');
  msg = msg + 'Mousedown event occurred';
  message.textContent = msg;
});
*/

/*
// Mouseenter event
document
  .getElementById("container")
  .addEventListener("mouseenter", function () {
    msg = msg + "Mouseenter event occurred";
    message.textContent = msg;
  });


// Mouseleave event
document.getElementById('container').addEventListener('mouseleave', function() {
  //alert('Mouse left the container');
  msg = msg + 'Mouseleave event occurred';
  message.textContent = msg;
});
*/

/*
// Mousemove event
let container = document.getElementById('container');
container.addEventListener('mousemove', function(event) {
  container.textContent = Mouse coordinates - X: ${event.clientX}, Y: ${event.clientY};
});
*/

/*
// Drag event
document.getElementById('target').addEventListener('drag', function() {
  //alert('Drag event occurred');
  msg = msg + 'Drag event occurred';
  message.textContent = msg;
});
*/

/*
// Mouseover event
document.getElementById('container').addEventListener('mouseover', function(event) {
  //alert('Mouseover event occurred');
  container.textContent = Mouse coordinates - X: ${event.clientX}, Y: ${event.clientY};
});
*/

/*
// Contextmenu event (right-click)

document.getElementById('container').addEventListener('contextmenu', function(event) {
  event.preventDefault(); // Prevent default context menu
  //alert('Context menu event occurred');
  msg = msg + 'Context menu event occurred';
  message.textContent = msg;
});
*/

// Mousewheel event
/*
container = document.getElementById('container');
container.addEventListener('wheel', function(event) {
  message.textContent = Mousewheel event occurred. Delta: ${event.deltaY};
});
*/